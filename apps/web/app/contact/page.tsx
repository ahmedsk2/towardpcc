import { PillarStub } from "@/components/pillar-stub";

export const metadata = { title: "Contact" };

export default function ContactPage() {
  return (
    <PillarStub
      title="Contact"
      status="in development"
      description="A contact form lands here soon. Until then, this page exists so you always know where to reach us once the platform launches."
    />
  );
}
