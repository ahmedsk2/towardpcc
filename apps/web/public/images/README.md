# Site photography

Drop the founder-supplied images here using these exact filenames. Each is
referenced by an `ImageSlot`, which renders the brand gradient underneath — so a
missing file degrades to a labelled placeholder rather than a broken image.

| File                     | Used on                           | Subject                                             |
| ------------------------ | --------------------------------- | --------------------------------------------------- |
| `care-nurse-smiling.jpg` | `/` mission split                 | nurse laughing with a child holding a teddy         |
| `care-teddy-oxygen.jpg`  | `/data`                           | teddy bear in a hospital bed wearing an oxygen mask |
| `care-thermometer.jpg`   | `/services`                       | clinician taking a child's temperature              |
| `brand-waveform.jpg`     | `/` and `/about` founder sections | abstract blush/plum gradient with a waveform        |
| `library-screenshot.png` | `/knowledge`                      | a real PedsCC Library browse/search view            |
| `care-resting.jpg`       | reserved                          | child asleep in a hospital bed with a teddy         |

## Deliberately NOT used

- **No founder portrait.** The founder chose not to publish one; the brand
  waveform carries that section instead.
- **Registry dashboard: use the redacted version only.** The first screenshot
  named the health cluster in its sidebar; that branding must stay removed. The
  operational figures it still shows (admissions, discharges, bed occupancy
  above 100%, admission dates) are real. They carry no patient identifiers and
  no institution is named, but they remain that unit's numbers — confirm they
  are yours to publish, or replace them with illustrative values.
- **No flat glowing horizontal line.** A flat line on a dark ground reads as a
  flatline, which ADR-design-direction explicitly rejects for a paediatric
  intensive care product. The signature waveform is respiratory and continuous.

Licensing: every photograph must be genuinely free-licensed or owned, and none
may be captioned or positioned to imply it depicts TowardPCC's own unit, staff
or patients (spec §7).
