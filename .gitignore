# dependencies
node_modules/
.pnpm-store/

# builds
.next/
out/
dist/
coverage/
*.tsbuildinfo
next-env.d.ts

# Serwist-generated service worker
apps/web/public/sw.js
apps/web/public/sw.js.map

# env — only .env.example is committed
.env
.env.*
!.env.example

# tooling
.turbo/
.vercel/
*.log
.DS_Store
Thumbs.db

# test artifacts
playwright-report/
test-results/
.lighthouseci/

# taskmanager volatile state (db itself is committed; WAL/logs are not)
.taskmanager/logs/
.taskmanager/*.db-wal
.taskmanager/*.db-shm

# personal project-scoped Claude Code settings
.claude/settings.local.json

# Validation agents write throwaway probes here and delete them; one was
# swept into a commit by `git add -A` on 2026-08-17. Never trackable.
packages/scoring-engine/src/scores/__probe_*.test.ts
